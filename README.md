## Getting Started

First, run the development server:

```bash
npm run dev
```

## Deploy

Run

```bash
npm run deploy
```

This deploy script with run the `next build` command and output the static site in a separate directory that is linked to Github Pages.
